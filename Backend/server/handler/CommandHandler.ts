import type { PrivmsgMessage } from '@kararty/dank-twitch-irc';

export async function CommandHandler(msg: PrivmsgMessage) {
	// const { channelName, messageText } = msg;
	// Bot.WS.Send(channelName, {
	// 	type: 'message',
	// 	channel: channelName,
	// 	message: messageText,
	// });
}
